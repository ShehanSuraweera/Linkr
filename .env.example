# Required — must be at least 32 characters
JWT_SECRET=replace-this-with-a-long-random-secret-min-32-chars

# Optional — overrides the backend URL baked into the frontend JS bundle.
# Change this when deploying to a public domain (e.g. https://api.yourdomain.com).
# NEXT_PUBLIC_API_URL=http://localhost:8080
